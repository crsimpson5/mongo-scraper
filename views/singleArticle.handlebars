<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
  <a class="navbar-brand" href="/">Mongo Scraper</a>
  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent"
    aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>

  <div class="collapse navbar-collapse" id="navbarSupportedContent">
    <ul class="navbar-nav mr-auto">
      <li class="nav-item">
        <a class="nav-link" href="/">Home</span></a>
      </li>
      <li class="nav-item">
        <a class="nav-link" href="https://github.com/crsimpson5/mongo-scraper">GitHub Repo</span></a>
      </li>
    </ul>
  </div>

  <div class="collapse navbar-collapse" id="navbarSupportedContent">
    <ul class="navbar-nav ml-auto">
      <li class="nav-item">
        <button class="btn btn-primary" id="scrape-btn">Scrape Articles</a>
      </li>
    </ul>
  </div>
</nav>

<div class="container">
  <div class="jumbotron jumbotron-fluid">
    <div class="jumbotron text-center">
      <h1 class="display-4">Mongo Scraper</h1>
      <p class="lead">theScore Esports Edition</p>
    </div>

    <div class="p-5">
      <div class="card border-dark" id="article" data-id="{{this._id}}">
        <div class="card-header bg-dark">
        </div>
        <div class="card-body">
          <h5 class="card-title"><a href={{this.url}}>{{this.headline}}</a></h5>
          <p class="card-text">{{this.summary}}</p>
        </div>
      </div>
      <br>
      <h4>Comments</h4>
      {{#each this.comments}}
      <div class="card border-dark">
        <div class="card-header bg-dark">
        </div>
        <div class="card-body">
          <p class="card-text">{{this.text}}
            <button class="btn btn-danger float-right delete-btn" data-id="{{this._id}}">Delete</button>
          </p>
          
        </div>
      </div>
      <br>
      {{/each}}
      <br>
      <form>
        <div class="form-group">
          <input type="text" class="form-control" id="comment-input" 
            placeholder="Add a comment">
        </div>
        <button type="submit" class="btn btn-primary" id="submit-btn">Submit</button>
      </form>
    </div>
  </div>
</div>